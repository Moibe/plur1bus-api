# Copia este archivo a `.env` (NO lo commitees).

# Orígenes del front permitidos por CORS (coma-separada). Default: Vite local
# (5173) y el puerto reservado del front (4400), en localhost y 127.0.0.1.
# CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:4400
