# Copia este archivo a `.env` (NO lo commitees).

# Orígenes del front permitidos por CORS (coma-separada). Sin definir, se acepta
# cualquier puerto de localhost y 127.0.0.1 (desarrollo local).
# CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:4400
